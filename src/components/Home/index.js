import React from 'react';
import MainView from './MainView';

class Home extends React.Component {
  constructor (props) {
    super(props);
  }

  render() {
    return(
      <MainView />
    );
  }
}

export default Home;
